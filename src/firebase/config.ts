// Firebase configuration - disabled for production deployment
// Set up environment variables in Netlify if you need Firebase functionality

// For now, disable Firebase completely to avoid deployment issues
const app: unknown = null;
const db: unknown = null;

export { app };
export { db };
